{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "test -z \"$(git show --pretty='' --name-only HEAD | grep -v '^control-center/' | sed '/^$/d')\"",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
