{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ -n \"${VERCEL_GIT_PREVIOUS_SHA:-}\" ] && git cat-file -e \"${VERCEL_GIT_PREVIOUS_SHA}^{commit}\" 2>/dev/null; then git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA}\" HEAD -- . ':(exclude)control-center/**'; else exit 1; fi",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
