{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)control-center/**'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
