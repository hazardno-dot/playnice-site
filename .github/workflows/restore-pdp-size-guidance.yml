name: Restore PDP size guidance

on:
  push:
    branches:
      - mobile-product-page-clean
    paths:
      - playnice-site/scripts/restore-pdp-size-guidance.cjs
      - .github/workflows/restore-pdp-size-guidance.yml

permissions:
  contents: write

jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: mobile-product-page-clean
          fetch-depth: 0
      - name: Apply guarded codemod
        run: node playnice-site/scripts/restore-pdp-size-guidance.cjs
      - name: Commit source changes
        run: |
          if git diff --quiet; then
            echo "No source changes produced."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add playnice-site/src/mobile-v2/product-page/MobileProductPage.jsx playnice-site/src/mobile-v2/product-page/MobileProductPage.css
          git commit -m "Restore live mobile PDP size guidance [skip ci]"
          git push origin HEAD:mobile-product-page-clean
