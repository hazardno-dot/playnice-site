name: Patch scent request speed once

on:
  push:
    paths:
      - '.github/workflows/scent-request-speed-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: scent-request-vote-guardrails
          fetch-depth: 1

      - name: Patch App.js
        run: python .github/scripts/scent-request-speed-patch.py

      - name: Install dependencies
        working-directory: playnice-site
        run: npm install --package-lock=false

      - name: Test
        working-directory: playnice-site
        run: npm test -- --watchAll=false

      - name: Production build
        working-directory: playnice-site
        env:
          DISABLE_ESLINT_PLUGIN: 'true'
        run: npx react-scripts build

      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add playnice-site/src/App.js
          if git diff --cached --quiet; then
            echo 'No App.js changes to commit.'
            exit 0
          fi
          git commit -m 'fix: speed up scent request retry UX'
          git push origin HEAD:scent-request-vote-guardrails
